---
phase: 01-landing-page-dojo-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/dojo/landingPreview/types.ts
  - frontend/src/features/dojo/landingPreview/content.ts
  - frontend/src/features/dojo/landingPreview/evaluators.ts
  - frontend/src/features/dojo/landingPreview/index.ts
autonomous: true
requirements:
  - LDP-01
  - LDP-02
  - LDP-03
must_haves:
  truths:
    - Visitor can choose one of the five approved landing-page preview modes.
    - Each preview mode has a compact interaction with real correctness logic.
    - Preview submission returns a verdict and short explanation instead of static marketing copy.
  artifacts:
    - path: frontend/src/features/dojo/landingPreview/types.ts
      provides: Shared contracts for preview modes, interactions, and verdicts.
    - path: frontend/src/features/dojo/landingPreview/content.ts
      provides: Hardcoded preview scenarios for the five approved modes.
    - path: frontend/src/features/dojo/landingPreview/evaluators.ts
      provides: Reusable correctness evaluators and explanation payloads.
  key_links:
    - from: frontend/src/features/dojo/landingPreview/content.ts
      to: frontend/src/features/dojo/landingPreview/evaluators.ts
      via: shared mode ids and expected answer payloads
      pattern: "mode: 'parsons'|'surgery'|'eli5'|'mental'|'bigo'"
    - from: frontend/src/features/dojo/landingPreview/index.ts
      to: frontend/src/features/dojo/landingPreview/types.ts
      via: exported preview contracts
      pattern: export
---

<objective>
Define the landing-page dojo preview foundation so downstream UI work has stable contracts, curated scenarios, and reusable validation logic.

Purpose: Prevent shallow implementation in `DojoSection.tsx` by establishing concrete preview data and mode-specific evaluators first.
Output: A dedicated `landingPreview` module with exported types, five preview scenarios, and verdict helpers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-landing-page-dojo-interaction/01-CONTEXT.md
@.planning/phases/01-landing-page-dojo-interaction/01-RESEARCH.md
@frontend/src/components/DojoSection.tsx
@frontend/src/features/dojo/ParsonsChallenge.tsx
@frontend/src/features/dojo/CodeSurgery.tsx
@frontend/src/features/dojo/ELI5Challenge.tsx
@frontend/src/features/dojo/MentalCompiler.tsx
@frontend/src/features/dojo/BigOBattle.tsx
@frontend/src/features/dojo/ChallengeIcons.tsx

<interfaces>
From `frontend/src/features/dojo/ChallengeIcons.tsx`:

```typescript
export const ChallengeIcons = {
    parsons: ParsonsIcon,
    surgery: SurgeryIcon,
    eli5: ELI5Icon,
    mental: MentalIcon,
    bigo: BigOIcon,
};
```

From `frontend/src/hooks/useReducedMotion.ts`:

```typescript
export const useReducedMotion = () => boolean;
export const getAnimationConfig = (prefersReducedMotion: boolean) => ({
  duration: number,
  distance: number,
  scale: number,
  rotation: number,
  staggerDelay: number,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define landing preview contracts and mode registry</name>
  <files>frontend/src/features/dojo/landingPreview/types.ts, frontend/src/features/dojo/landingPreview/content.ts, frontend/src/features/dojo/landingPreview/index.ts</files>
  <read_first>frontend/src/components/DojoSection.tsx, frontend/src/features/dojo/ChallengeIcons.tsx, .planning/phases/01-landing-page-dojo-interaction/01-CONTEXT.md</read_first>
  <action>Create a new `frontend/src/features/dojo/landingPreview/` module that formalizes the landing preview scope per D-01 through D-04. Export a `LandingPreviewMode` union with exactly `parsons | surgery | eli5 | mental | bigo`, card metadata for those five modes only, and typed preview scenario payloads for each mode. In `content.ts`, add one short hardcoded scenario per mode with concrete prompts, options or sample text, correct-answer data, explanation copy, retry label, and next-preview label. Do not include the other five dojo modes and do not recreate the full `DojoPage` state model.</action>
  <acceptance_criteria>`frontend/src/features/dojo/landingPreview/types.ts` exports a five-mode union; `frontend/src/features/dojo/landingPreview/content.ts` contains exactly five preview definitions keyed by those modes; `frontend/src/features/dojo/landingPreview/index.ts` re-exports the public landing-preview contracts for downstream imports.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Downstream implementation can import one typed source of truth for selector cards and preview scenario content without re-reading the full dojo code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reusable verdict helpers for the five preview modes</name>
  <files>frontend/src/features/dojo/landingPreview/evaluators.ts, frontend/src/features/dojo/landingPreview/content.ts, frontend/src/features/dojo/landingPreview/index.ts</files>
  <read_first>frontend/src/features/dojo/ParsonsChallenge.tsx, frontend/src/features/dojo/CodeSurgery.tsx, frontend/src/features/dojo/ELI5Challenge.tsx, frontend/src/features/dojo/MentalCompiler.tsx, frontend/src/features/dojo/BigOBattle.tsx, frontend/src/features/dojo/landingPreview/types.ts, frontend/src/features/dojo/landingPreview/content.ts</read_first>
  <action>Implement pure mode-specific evaluator functions that take the compact preview input and return `{ isCorrect, explanation, retryLabel, nextPreviewLabel }` per D-05 and D-06. Use concrete checks suited to each mode: ordered id comparison for Parsons, bug-line match for Surgery, jargon-aware minimum-content check for ELI5, expected output option comparison for Mental Compiler, and normalized complexity comparison for Big O. Reuse existing wording patterns from the full challenge components where it helps, but keep explanations short enough for a landing section.</action>
  <acceptance_criteria>`frontend/src/features/dojo/landingPreview/evaluators.ts` exports one evaluator per approved mode plus a shared dispatcher; evaluator return values always include `isCorrect` and `explanation`; incorrect verdicts do not rely on score, streak, timer, or full-dojo-only copy.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The landing-page UI can request a verdict for any of the five preview modes without embedding mode logic directly inside `DojoSection.tsx`.</done>
</task>

</tasks>

<verification>
- Run `npm run build` in `frontend/` after the new `landingPreview` module is added.
- Confirm the exported preview module is limited to the five approved modes and has no references to `/learn` routing or the other dojo challenge types.
</verification>

<success_criteria>
- The codebase contains a dedicated landing-preview contract layer.
- The five preview modes have curated scenario content and reusable verdict helpers.
- Downstream UI work can consume typed preview data without rediscovering challenge logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-dojo-interaction/01-01-SUMMARY.md`
</output>
