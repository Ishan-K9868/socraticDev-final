---
phase: 01-landing-page-dojo-interaction
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/features/dojo/landingPreview/LandingDojoPreview.tsx
  - frontend/src/features/dojo/landingPreview/index.ts
  - frontend/src/components/DojoSection.tsx
autonomous: true
requirements:
  - LDP-01
  - LDP-02
  - LDP-03
  - LDP-04
  - LDP-05
must_haves:
  truths:
    - Visitor can switch between the five preview cards from the landing page.
    - Visitor can solve a mini challenge inline and immediately see whether the answer is correct.
    - After feedback, visitor can retry, move to another preview, or enter `/dojo`.
    - The section still behaves cleanly on mobile and respects reduced motion.
  artifacts:
    - path: frontend/src/features/dojo/landingPreview/LandingDojoPreview.tsx
      provides: Shared inline preview panel that renders the active mode interaction.
    - path: frontend/src/components/DojoSection.tsx
      provides: Landing section shell, clickable mode cards, and `/dojo` handoff actions.
  key_links:
    - from: frontend/src/components/DojoSection.tsx
      to: frontend/src/features/dojo/landingPreview/LandingDojoPreview.tsx
      via: active preview mode props and verdict callbacks
      pattern: LandingDojoPreview
    - from: frontend/src/components/DojoSection.tsx
      to: /dojo
      via: Link components for primary handoff actions
      pattern: to="/dojo"
---

<objective>
Replace the current landing dojo rotator with a genuine five-mode interactive preview and clear handoff into the full dojo.

Purpose: Deliver the user-facing phase outcome by wiring the preview contracts into the landing page without expanding scope beyond the selected five compact interactions.
Output: Shared preview UI plus an updated `DojoSection.tsx` that uses clickable cards, inline validation, and `/dojo` actions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-landing-page-dojo-interaction/01-CONTEXT.md
@.planning/phases/01-landing-page-dojo-interaction/01-RESEARCH.md
@.planning/phases/01-landing-page-dojo-interaction/01-01-PLAN.md
@frontend/src/components/DojoSection.tsx
@frontend/src/ui/Button.tsx
@frontend/src/hooks/useReducedMotion.ts
@frontend/src/features/dojo/landingPreview/types.ts
@frontend/src/features/dojo/landingPreview/content.ts
@frontend/src/features/dojo/landingPreview/evaluators.ts

<interfaces>
From `frontend/src/ui/Button.tsx`:

```typescript
interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
    variant?: 'primary' | 'secondary' | 'ghost' | 'danger';
    size?: 'sm' | 'md' | 'lg';
    icon?: ReactNode;
    iconPosition?: 'left' | 'right';
    isLoading?: boolean;
    fullWidth?: boolean;
}
```

From `frontend/src/components/DojoSection.tsx` existing challenge metadata:

```typescript
interface Challenge {
    id: keyof typeof ChallengeIcons;
    name: string;
    tagline: string;
    description: string;
    color: string;
    bgColor: string;
    borderColor: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared landing preview panel</name>
  <files>frontend/src/features/dojo/landingPreview/LandingDojoPreview.tsx, frontend/src/features/dojo/landingPreview/index.ts</files>
  <read_first>frontend/src/features/dojo/landingPreview/types.ts, frontend/src/features/dojo/landingPreview/content.ts, frontend/src/features/dojo/landingPreview/evaluators.ts, frontend/src/ui/Button.tsx, frontend/src/hooks/useReducedMotion.ts</read_first>
  <action>Create `LandingDojoPreview.tsx` as a compact shared preview surface that renders the active mode's prompt and input controls while keeping state local. Support the five required interactions per D-01 and D-02: draggable or reorder-style selection for Parsons, line-choice bug pick for Surgery, short textarea submission for ELI5, multiple-choice output selection for Mental Compiler, and multiple-choice complexity selection for Big O. On submit, call the shared evaluators and render immediate verdict UI with explanation copy plus `Try again` and `Next preview` actions per D-05 through D-07. Export this component from the landing-preview barrel.</action>
  <acceptance_criteria>`frontend/src/features/dojo/landingPreview/LandingDojoPreview.tsx` accepts an active mode and scenario data; it renders submit + verdict states for all five modes; post-submit controls include both retry and next-preview affordances; reduced-motion users do not depend on large transforms to complete the interaction.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A single reusable preview component exists and can power the landing dojo interaction without importing the full-screen dojo pages.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the landing dojo section around clickable cards and `/dojo` handoff</name>
  <files>frontend/src/components/DojoSection.tsx, frontend/src/features/dojo/landingPreview/index.ts</files>
  <read_first>frontend/src/components/DojoSection.tsx, frontend/src/features/dojo/landingPreview/LandingDojoPreview.tsx, frontend/src/features/dojo/landingPreview/content.ts, .planning/phases/01-landing-page-dojo-interaction/01-CONTEXT.md</read_first>
  <action>Update `DojoSection.tsx` to remove the auto-rotating featured challenge and the static 10-mode emphasis. Replace them with five clickable selector cards per D-03 and D-04, all driven from the landing-preview registry. Mount the shared `LandingDojoPreview` beneath the card grid, keep the premium landing styling direction, and ensure the verdict area offers another try plus a next-preview action per D-06 and D-07. Change the main CTA route to `/dojo` per D-08, keep the section responsive across mobile and desktop, and avoid adding any scoreboard-first UI or the deferred extra challenge modes.</action>
  <acceptance_criteria>`frontend/src/components/DojoSection.tsx` contains exactly five selector cards for the approved modes; there is no interval-based auto-rotation left in the section; the primary handoff CTA uses `to="/dojo"`; the section renders inline preview feedback and retains reduced-motion-safe Framer Motion usage.</acceptance_criteria>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>The landing page dojo section becomes a real interactive preview with clear retry, next-preview, and `/dojo` handoff paths.</done>
</task>

</tasks>

<verification>
- Run `npm run build && npm run lint` in `frontend/`.
- In the browser, verify `/` shows five clickable dojo preview cards, each preview can be answered, and the main CTA goes to `/dojo`.
- Spot check one correct and one incorrect submission to confirm explanation-first feedback rather than score-first messaging.
</verification>

<success_criteria>
- The landing-page dojo section no longer behaves like a static rotator.
- All five approved previews are interactive and validated inline.
- Visitors can retry, continue to another preview, or enter `/dojo` from the same section.
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page-dojo-interaction/01-02-SUMMARY.md`
</output>
